{{- if .Values.registryService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: registry-service
spec:
  replicas: {{ .Values.registryService.replicaCount }}
  selector:
    matchLabels:
      {{- include "agent-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: registry-service
  template:
    metadata:
      labels:
        {{- include "agent-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: registry-service
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "agent-platform.serviceAccountName" . }}
      containers:
        - name: registry-service
          image: "{{ include "agent-platform.imageRegistry" . }}/{{ .Values.registryService.image.repository }}:{{ .Values.registryService.image.tag }}"
          imagePullPolicy: {{ .Values.registryService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.registryService.service.port }}
          env:
            {{- include "agent-platform.commonEnv" . | nindent 12 }}
            - name: SERVICE_NAME
              value: "registry-service"
          envFrom:
            - configMapRef:
                name: platform-config
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml .Values.registryService.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: registry-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: registry-service
spec:
  type: {{ .Values.registryService.service.type }}
  ports:
    - port: {{ .Values.registryService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: registry-service
{{- end }}
